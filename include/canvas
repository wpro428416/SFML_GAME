#ifndef CANVAS_H_INCLUDED
#define CANVAS_H_INCLUDED

#include "heatTest.h"
#include <SFML/Graphics.hpp>


#include "initia.h"

class Canvas
{
private:
    int launch;//用来判断敌机发射子弹
    sf::RenderWindow & window;
    int tab[200];//用来坐敌机爆炸的标记
public:

    int mark;
    Canvas(sf::RenderWindow & window);
    ~Canvas();
    static void append(sf::Vector2f x_y1,int mark);
    void draw();
};


#endif // CANVAS_H_INCLUDED
